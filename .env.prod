# Production Environment Configuration (Docker Mode)
PORT=8080
HOST=0.0.0.0
APP_ENV=production
APP_TITLE="Constitution of Zimbabwe"
DOCKER_IMAGE=turbulance/zim-constitution:latest
RESTART_POLICY=unless-stopped
